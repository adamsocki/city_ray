{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run raylib",
            "type": "node-terminal",
            "request": "launch",
            "command": "make && ./hello",
            "cwd": "${workspaceFolder}"
        }
    ]
}
